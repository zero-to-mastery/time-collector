import math

sum = 0;
inputData = (str(open('data.txt', 'r').read())).split("\n")

values = (math.floor(n/3) - 2)
while values

for n in inputData[0:100]:
    n = int(n)
    values = (math.floor(n/3) - 2)
    sum += values

print(sum)
